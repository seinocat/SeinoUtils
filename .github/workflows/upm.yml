name: Release Workflow

on:
  push:
    tags:
      - '*'
      
permissions:
  contents: write
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref : 'upm'
        
      - name: Release
        id: create_release
        run: gh release create ${{ github.ref }} ./path/to/release/assets/* --target upm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
